[pytest]
# `python -m pytest` puts the CWD on sys.path but bare `pytest` does not, so tests that pass
# locally failed in CI with "No module named 'app'". Pinning it here makes both invocations
# behave the same.
pythonpath = .
testpaths = tests
